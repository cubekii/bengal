[package]
name = "sparkler"
version = "0.1.0"
edition = "2021"
description = "Virtual machine for Bengal language"

[dependencies]
tokio = { version = "1", features = ["full"] }
async-recursion = "1.1"
serde = { version = "1.0", features = ["derive"] }
simd-json = "0.13"
