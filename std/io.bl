module std.io

native fn print(text: str) {}

fn println<T>(line: T) {
  print(str(line) + "\n")
}

native fn sleep(ms: int) {}

native fn readLine(): str {}

interface InputStream {
  fn read(size: int): ByteBuffer
}

interface OutputStream {
  //fn write(todo)
}
