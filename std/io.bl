module std.io

native fn print(text: str) {}

fn println(line: str) {
  print(line + "\n")
}

async native fn sleep(ms: int) {}

async native fn readLine(): str {}

interface InputStream {
  async fn read(size: int): ByteBuffer
}

interface OutputStream {
  //async fn write(todo)
}
